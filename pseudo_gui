# Initialize everything

# def struct_params(struct_filename)
# function for extracting and modifying parameters based on input structure filename

# Initialize GUI with default structure file
    # default strucutre file is set here
    # only the usable parameters from dictionary = struct_params(struct_filename)
    
# def create_gui(parameters)
    # return show the GUI with parameters loaded in
    
    # "Open" button --> open file dialog --> extract structure filename --> struct_params(struct_filename) --> rerun create_gui(parameters)
    # "Save" button --> save parameters to same structure file (overwrite)
    # "Save As" button --> save parameters to new structure file. Prompt filesave dialogue.
    
    # "Mechanical Impedances" --> pass parameters to mechanical_impedances.py --> Plot mechanical impedances
    # "Electrical Impedances" --> pass parameters to electrical_impedances.py --> Plot electrical impedances
    # "Transfer Functions" --> pass parameters to transfer_functions.py --> Plot transfer functions